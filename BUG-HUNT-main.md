# AWS Mapper - Bug Hunt Report (branch: main)

Date: 2026-05-29  |  Method: adversarial multi-agent hunt across 10 subsystem domains (two passes), each finding independently re-verified against raw file bytes (rtk-proxy-resistant). Only findings that survived a refutation pass are listed.

Status: **RESOLVED on branch `refactor/module-ownership`** (2026-06-02). All 18 are
fixed: #1/#2/#4/#8 earlier; #3/#5/#6/#7/#9/#10/#11/#12/#13/#14/#15/#16/#17 in commit
`dc96dfd` (tests-first; live app-core inline + module twins patched in lockstep);
#18 was already fixed by `33bb0c0`. See `HUMIFY-PLAN.md` → "Execution status" for the
per-bug mapping and the regression tests. Not yet merged to `main`.

## Summary

- Confirmed bugs: **18**  (0 critical, 5 high, 7 medium, 6 low)
- Coverage: all 10 domains hunted (counts/off-by-one, reports-render, compliance-engine, region-ingest, multi-account/state, IAM/governance, map-flow, exports, BUDR, IaC). Domains with no confirmed bugs: compliance-engine, region-ingest, iac-misc.

### Notes on specific user-reported items
- **Map finding count "shows 5 for 4":** most likely bug #8 (map badge ignores the active account filter and counts findings from filtered-out accounts in multi-account mode). No literal "counter starts at 1" bug was found, so if you also see 5-for-4 in **single-account** mode, that specific case needs a fresh repro.
- **IAM "blank findings":** confirmed as the IAM Review state.js desync bug below (counts read a never-populated binding). The em-dash/`0` display change already on main is cosmetic only; this is the real fix.

## Confirmed bugs

### 1. [HIGH] XLSX inventory sheet ignores the account filter for 7 resource types (peerings, zones, WAF, CloudFront, VPN, TGW attachments, target groups)

- **Domain:** exports
- **Location:** `src/exports/exports-xlsx.js` (lines 513-538 (filtered loops above at 443-512; _af def at 434-437))
- **Impact:** When the user selects a specific account in the report's account filter and exports XLSX, the Inventory sheet still includes VPC peerings, Route 53 zones, WAF ACLs, CloudFront distributions, VPNs, TGW attachments, and target groups from ALL loaded accounts. The on-screen report and the DOCX export filter these same types correctly (the canonical _inventoryData carries an account label and is filtered via _rptFilterByAccount, which matches on item.account; DOCX uses _daf for its subset). So the XLSX sheet shows rows for accounts the user explicitly filtered out — a data-completeness/correctness mismatch versus the screen.
- **Defect:** In _rptBuildXlsxInventory the first 19 resource types are emitted via _af(c.xxx) which applies the report account filter (defined at line 434: filters by r._accountId===acctFilter). But starting at line 513 the code switches to raw iteration: (c.peerings||[]).forEach(...), and likewise for c.zones (517), c.wafAcls (522), c.cfDistributions (526), c.vpns (529), c.tgwAttachments (532), and c.tgs (535). These 7 loops never call _af(), so the account filter is silently bypassed for them.
- **Suggested fix:** Wrap each of the 7 trailing loops in _af(), matching the 19 loops above: _af(c.peerings).forEach(...), _af(c.zones).forEach(...), _af(c.wafAcls).forEach(...), _af(c.cfDistributions).forEach(...), _af(c.vpns).forEach(...), _af(c.tgwAttachments).forEach(...), _af(c.tgs).forEach(...). Each object carries _accountId, so _af() will filter correctly.
- **Verification:** Confirmed and reproducible. In _rptBuildXlsxInventory (src/exports/exports-xlsx.js), the account filter is implemented by _af() (lines 434-437): when acctFilter is a specific account it returns (arr||[]).filter(r => (r._accountId||'')===acctFilter). The first 19 resource loops (raw-verified lines 443-512) all route through _af(c.xxx). But the 7 trailing loops (raw-verified lines 513-538) iterate the raw arrays directly: (c.peerings||[]).forEach (513), (c.zones||[]) (517), (c.wafAcls||[]) (522), (c.cfDistributions||[]) (526), (c.vpns||[]) (529), (c.tgwAttachments||[]) (532), (c.tgs||[]) (535). None call _af(), so the account filter is bypassed for these 7 types.

The context c=_rlCtx is multi...

### 2. [HIGH] IAM Review per-principal findings always empty (reads never-populated state.js complianceFindings)

- **Domain:** iam-governance
- **Location:** `src/modules/governance.js` (lines 593 and 606 (import line 5; root cause state.js:35 setComplianceFindings never called; engine writes only window._complianceFindings at compliance-engine.js:553/558/562))
- **Impact:** The IAM Review tab's 'With Findings' summary card (app-core.js:16931), the per-row Findings badge (app-core.js:16959), the expandable Findings list (app-core.js:16973-16980), and the HTML report's 'With Findings' KPI + Findings columns (report-html.js:1183, 1191, 1201, 1211, 1225) all show 0/'—' even when runIAMChecks generated CRITICAL/HIGH IAM findings (e.g. AdminRole IAM-1, dev-user no-MFA IAM-3). The IAM detail panel (app-core.js:2653-2658) reads the correct window._complianceFindings, so it DOES show findings — producing a visible contradiction between the panel and the Review tab/report for the same principal.
- **Defect:** prepareIAMReviewData() builds each role/user's `findings` array by filtering the module-scoped `complianceFindings` imported from ./state.js (line 5: `import { rlCtx, complianceFindings } from './state.js'`). Lines 593/606 do `(complianceFindings || []).filter(f => f.framework==='IAM' && ...)`. But state.js's `complianceFindings` is initialized to `[]` (state.js:21) and is ONLY ever reassigned via setComplianceFindings (state.js:35), which is NEVER called anywhere in the codebase (verified across src/ and confirmed tree-shaken out of dist/app-core.js). The actual IAM findings produced by runIAMChecks live in compliance-engine's local `_complianceFindings` / `window._complianceFindings`, a DIFFERENT array. So every principal gets `findings: []`.
- **Suggested fix:** Make prepareIAMReviewData read the populated source. Option (a): in compliance-engine.js runComplianceChecks, after computing _complianceFindings, call State.setComplianceFindings(_complianceFindings) so the state.js binding stays in sync (requires importing State; also fixes the identical empty-binding defect in _buildInventoryData's compMap at governance.js:271). Option (b): change governance.js lines 593 and 606 (and 271) to read window._complianceFindings (the already-populated array) instead of the imported state binding. Verified that runIAMChecks sets f.resource to RoleName/Arn so the existing filter predicate would then match correctly.
- **Verification:** Raw-byte verified end to end. governance.js line 5 imports `complianceFindings` from ./state.js. prepareIAMReviewData (line 575) builds each principal's findings at line 593 (roles) and line 606 (users) via `(complianceFindings || []).filter(f => f.framework==='IAM' && (f.resource===role.RoleName || f.resource===(role.Arn||'')))`. The state.js binding is initialized to `[]` (state.js:21) and ONLY mutated by setComplianceFindings (state.js:35). I grepped all of src/: the sole occurrence of setComplianceFindings is its definition — it is NEVER called. compliance-engine.js (runComplianceChecks, line 549) does NOT import State and writes IAM findings only to its own `_complianceFindings` / `wind...

### 3. [HIGH] Cross-VPC TGW reachability uses OR; never requires both VPCs on the same Transit Gateway

- **Domain:** map-flow
- **Location:** `src/modules/flow-tracing.js` (lines 547-552)
- **Impact:** Flow tracing reports false connectivity (ALLOW) between two VPCs when only one of them is attached to a transit gateway, or when each is attached to a DIFFERENT transit gateway. The flow path shows a green 'Transit Gateway route between VPCs' hop and an allowed target for traffic that AWS would actually drop, misleading the user about real cross-VPC reachability.
- **Defect:** In traceFlow's cross-VPC connectivity branch, when no VPC peering connects the two VPCs, it falls back to a TGW check: `(ctx.tgwAttachments||[]).forEach(att => { if (att.ResourceId === srcPos.vpcId || att.ResourceId === tgtPos.vpcId) tgwRoute = true; });` (lines 548-550). This sets tgwRoute=true if EITHER the source OR the target VPC has ANY transit-gateway attachment. It never verifies that BOTH VPCs are attached, nor that they share the SAME TransitGatewayId. Because the source-side route table check (rtResultX, line ~525) already guarantees the source has a non-blackhole route to the target (commonly via its TGW attachment), the OR condition is effectively always true whenever the source is TGW-attached. The function then emits hop `{type:'tgw', action:'allow', detail:'Transit Gateway route between VPCs'}` and proceeds to mark the target reachable.
- **Suggested fix:** Require both VPCs on the same TGW before emitting the allow hop: `const srcTgws=new Set(); const tgtTgws=new Set(); (ctx.tgwAttachments||[]).forEach(function(att){ if(att.ResourceId===srcPos.vpcId) srcTgws.add(att.TransitGatewayId); if(att.ResourceId===tgtPos.vpcId) tgtTgws.add(att.TransitGatewayId); }); const tgwRoute=[...srcTgws].some(function(t){return tgtTgws.has(t);});` then push the allow hop only when tgwRoute is true; otherwise fall through to the existing block branch. (When also supporting the multi-account producer, match on att.ResourceId||att.VpcId.)
- **Verification:** Verified verbatim with sed. flow-tracing.js:547-552 is the only TGW logic in the file (grep confirms no other TransitGatewayId/ResourceId references). Lines 548-549: `(ctx.tgwAttachments||[]).forEach(function(att){ if (att.ResourceId===srcPos.vpcId || att.ResourceId===tgtPos.vpcId) tgwRoute=true; });` sets tgwRoute=true when EITHER VPC has ANY TGW attachment. It never (a) requires BOTH VPCs attached, nor (b) checks they share the same TransitGatewayId. The tgwAttachments element shape (cidr-engine.js:444-450 and demo-data.js:440-446) carries both ResourceId (the VPC) and TransitGatewayId, so the per-VPC OR genuinely matches a single VPC's attachment. Trace: the cross-VPC branch first runs th...

### 4. [HIGH] mergeContexts never recomputes _multiAccount (hardcoded true) — single-account multi-region merges falsely render multi-account UI

- **Domain:** multiaccount-state
- **Location:** `src/app-core.js` (lines app-core.js:10426 (init _multiAccount:true) and app-core.js:10477 (`merged._multiRegion=merged._regions.size>1;` with no _multiAccount recompute); mirrored at src/modules/multi-account.js:200 and :239)
- **Impact:** When a user merges multiple REGIONS of a SINGLE account (the documented `_isRegion:true` flow at app-core.js:8741/8778, e.g. 'profileName / regionName' contexts), `merged._accounts.size === 1` but `_multiAccount` stays true. Downstream consumers gated on `_rlCtx._multiAccount` then render account-specific UI that should not appear: the 'Accounts' stat chip (app-core.js:8254), per-VPC `[accountId]` label tags in topology (app-core.js:4830 and 7628), and search-result account columns (app-core.js:9133). The merged single-account view is mislabeled as multi-account.
- **Defect:** The merged context object is initialized with `_multiAccount: true` and, at the end of mergeContexts, only `_multiRegion` is recomputed (`merged._multiRegion = merged._regions.size > 1`). `_multiAccount` is never recomputed from `merged._accounts.size`. This is asymmetric with buildRlCtxFromData, which correctly derives `const _multiAccount = _accounts.size > 1` (app-core.js:10335, multi-account.js:114). The merge accumulates `merged._accounts.add(ctx.accountId)` for every visible context, so its true cardinality is known but ignored.
- **Suggested fix:** At the end of mergeContexts, immediately before `return merged;`, add the missing recompute next to the existing _multiRegion line. In app-core.js (~10477): `merged._multiAccount=merged._accounts.size>1;`. In src/modules/multi-account.js (~239): `merged._multiAccount = merged._accounts.size > 1;`. Optionally change the initializer to `_multiAccount:false` for clarity since the value is now always derived. This mirrors buildRlCtxFromData's `_multiAccount=_accounts.size>1` and uses the already-accumulated merged._accounts set.
- **Verification:** Verified with /usr/bin/sed in both the executed copy (src/app-core.js) and the module copy (src/modules/multi-account.js).

ASYMMETRY (verified raw):
- buildRlCtxFromData derives the flag correctly: app-core.js:10333 `const _multiAccount=_accounts.size>1;` and multi-account.js:114 `const _multiAccount = _accounts.size > 1;`.
- mergeContexts hardcodes it: app-core.js:10426 `...recsByZone:{},_multiAccount:true,_accounts:new Set(),_regions:new Set(),_multiRegion:false,` and multi-account.js:200 `..._multiAccount: true, _accounts: new Set(), _regions: new Set(), _multiRegion: false`.
- At the end of mergeContexts only _multiRegion is recomputed: app-core.js:10477 `merged._multiRegion=merged._reg...

### 5. [HIGH] BUDR Assessment table: Tier column header sort is broken (label/key mismatch in rptSort)

- **Domain:** reports-render
- **Location:** `src/modules/report-html.js` (lines 515 (tier comparator) and 409 (TIER_ORD); cell text rendered at 1154 (label built at 1144); header at 1135; cell-text source at 511)
- **Impact:** Clicking the 'Tier' column header in the BUDR Assessment table appears to do nothing useful: rows are NOT sorted by protection tier (At Risk / Partial / Protected). The arrow indicator flips but the order is unchanged, so users trying to group at-risk resources by sorting get incorrect/unsorted results. The initial server-side render is correct (it uses tierOrder keyed on the raw keys), so the bug only manifests on user interaction.
- **Defect:** In the interactive report's client-side sort (rptSort, line 515), the Tier column comparator does cmp=(TIER_ORD[av]...) where TIER_ORD={at_risk:0,partial:1,protected:2} (line 409). But av is the cell's textContent. The Tier cell in _rptBUDRAssessmentTable renders the HUMAN LABEL, not the key: tierLabel={protected:'Protected',partial:'Partial',at_risk:'At Risk'}[tier] (line 1144) emitted at line 1153. So av is 'Protected'/'Partial'/'At Risk', and TIER_ORD['Protected'], TIER_ORD['Partial'], TIER_ORD['At Risk'] are ALL undefined -> all coerced to 9. Every row compares equal, so clicking the Tier header produces no meaningful reorder (rows stay in their existing order regardless of asc/desc). Raw-verified: line 514-516 show severity uses SEV_ORD[av] with uppercase cell text that matches its keys, and strategy lowercases; only tier has neither normalization nor matching keys. CSS text-transform does not affect textContent.
- **Suggested fix:** In rptSort, make the tier branch lookup match the rendered text. Either (a) key TIER_ORD by lowercased human label and normalize, e.g. TIER_ORD={'at risk':0,'partial':1,'protected':2} with TIER_ORD[av.toLowerCase()]; or (b) sort off the row's already-correct data-tier attribute instead of cell text, e.g. compare a.closest('tr').dataset.tier / a.dataset.tier against the snake_case-keyed TIER_ORD. Option (b) is most robust since data-tier (line 1150) already holds the raw key 'at_risk'/'partial'/'protected'. Verify the column index path still works if switching from children[idx].textContent to a dataset lookup.
- **Verification:** Raw-verified via /usr/bin/sed and python3 readlines (proxied and raw reads agreed). The client-side sort comparator rptSort reads the cell's textContent (line 511: var av=a.children[idx].textContent.trim()) and, for the tier branch (line 515), looks it up directly in TIER_ORD without normalization. TIER_ORD (line 409) is keyed on snake_case keys {at_risk:0,partial:1,protected:2}. But the Tier cell in _rptBUDRAssessmentTable renders the human label, not the key: tierLabel={protected:'Protected',partial:'Partial',at_risk:'At Risk'}[tier] (line 1144) emitted at line 1154 as esc(tierLabel). So av is 'Protected'/'Partial'/'At Risk', and TIER_ORD['Protected'], TIER_ORD['Partial'], TIER_ORD['At Ris...

### 6. [MEDIUM] BUDR signal badge inverts EBS SnapAgeDays coloring (fresh snapshot shown ✗, stale shown ✓)

- **Domain:** budr
- **Location:** `src/app-core.js` (lines 16356-16359)
- **Impact:** Operators reading the BUDR dashboard see a recently-snapshotted EC2 volume flagged with a red ✗ 'SnapAgeDays: 0' badge (looks like a failure), while a dangerously stale snapshot gets a reassuring green ✓. The single most informative backup signal for EC2 is shown with inverted meaning, misleading DR readiness judgments.
- **Defect:** In the BUDR dashboard signal-badge renderer, the good/bad classification for numeric signals is: `var good=v===true||(typeof v==='number'&&v>1); var bad=v===false||v===0;`. The EC2 assessment emits a numeric `SnapAgeDays` signal (set in budr-engine.js line ~162: `SnapAgeDays:snapAgeDays`). When the newest EBS snapshot was taken today, `snapAgeDays===0`, so the badge is classified as `bad` and rendered with a red ✗ — signalling danger even though a fresh snapshot is the healthiest possible state. Conversely, a STALE snapshot age (e.g. SnapAgeDays=30, which is bad) satisfies `v>1` and is classified `good` (green ✓). The age metric is treated as if larger were better, which is backwards. Raw-verified: budr-engine.js line 162 pushes SnapAgeDays; app-core.js lines 16355-16358 contain the classification.
- **Suggested fix:** Special-case SnapAgeDays in the badge classifier instead of using the generic v>1/v===0 heuristic. Lower age is better: e.g. when k==='SnapAgeDays', good = (typeof v==='number' && v<=7), bad = (v===null || v>7), warn otherwise. This aligns the badge with the existing BUDR-AGE-1 finding threshold (>7 days). At minimum, stop mapping v===0 to bad and v>1 to good for this field. Consider also flagging SnapAgeDays===null (no snapshot) as bad rather than warn.
- **Verification:** Raw-verified. app-core.js lines 16356-16359 classify each BUDR signal with a generic numeric heuristic: good = v===true || (typeof v==='number' && v>1); bad = v===false || v===0; then icon = good?'✓':bad?'✗':'!'. budr-engine.js line 149 computes snapAgeDays = newestSnap ? Math.floor((Date.now()-newestSnap)/864e5) : null, and line 161 pushes it as the numeric signal SnapAgeDays. For age, LOWER is better, so the heuristic is backwards: SnapAgeDays===0 (snapshot taken today, the healthiest state) matches v===0 and is classified bad -> red ✗; SnapAgeDays===30 (stale) matches v>1 and is classified good -> green ✓. The codebase itself confirms higher age is worse: budr-engine.js line 150 raises a ...

### 7. [MEDIUM] XLSX BUDR sheet mis-sorts 'At Risk' resources to the bottom (0 || 9 falsy bug)

- **Domain:** budr
- **Location:** `src/exports/exports-xlsx.js` (lines 406-408 (tierOrder declared at 406, comparator at 408; candidate cited 407-410))
- **Impact:** In the exported BUDR XLSX sheet, the highest-risk ('At Risk') resources — the ones a reviewer most needs to see first — are pushed to the bottom of the sheet beneath Partial and Protected rows, contradicting the dashboard and HTML report ordering and burying the critical findings.
- **Defect:** In `_rptBuildXlsxBUDR`, the sort comparator is `return (tierOrder[a.profile?a.profile.tier:'']||9)-(tierOrder[b.profile?b.profile.tier:'']||9);` with `tierOrder={at_risk:0,partial:1,protected:2}` (line 407). For an `at_risk` resource the lookup is `0`, and `0 || 9` evaluates to `9`, so at_risk rows sort LAST instead of first. Effective order becomes partial(1), protected(2), at_risk(9). The report-html version of the same sort (report-html.js line 1121) correctly uses a ternary `a.profile ? tierOrder[a.profile.tier] : 9` which preserves 0, confirming the intended order is at_risk-first. Raw-verified: tierOrder at line 407, comparator at lines 408-410.
- **Suggested fix:** Stop coercing the legitimate 0 key with `|| 9`. Mirror the ternary form in report-html.js: var oa = a.profile ? tierOrder[a.profile.tier] : 9; var ob = b.profile ? tierOrder[b.profile.tier] : 9; return oa - ob; (Optionally guard for an unknown tier with `!= null`, but in practice tier is always one of the three mapped keys.)
- **Verification:** Confirmed in raw bytes. In _rptBuildXlsxBUDR (src/exports/exports-xlsx.js), tierOrder={at_risk:0,partial:1,protected:2} is at line 406 (candidate said 407 — minor misquote), and the sort comparator at line 408 is: return (tierOrder[a.profile?a.profile.tier:'']||9)-(tierOrder[b.profile?b.profile.tier:'']||9);

For any real assessment, a.profile is truthy (every push sets profile to a _BUDR_RTO_RPO.* object — verified at budr-engine.js lines 131,161,172,176), so tierOrder[a.profile.tier] is evaluated. tier is always exactly 'at_risk' | 'partial' | 'protected' (the only three values in _BUDR_RTO_RPO, budr-engine.js lines 23-46), so the map never returns undefined. The defect is the `|| 9`: at_r...

### 8. [MEDIUM] Map compliance badge ignores active-account filter, over-counting findings in multi-account mode

- **Domain:** counts-offbyone
- **Location:** `src/app-core.js` (lines 9885-9898 (_buildComplianceLookup, no account filter); 9905-9976 (_renderComplianceBadges + VPC rollup); contrast 435 (_renderCompDash uses _udashFilterByAccount); 9434-9436 (_gatherResourceInfo spotlight, also account-agnostic); other shared-bug consumers 11473, 11992)
- **Impact:** In multi-account loads with the account filter active, the map badge shows a HIGHER finding count than the compliance panel / spotlight / report for the same node, because it includes findings from accounts the user has filtered out. The badge ceases to be a trustworthy at-a-glance count and disagrees with every other surface.
- **Defect:** The on-map per-node/per-VPC compliance badge derives its number from _buildComplianceLookup(), which iterates the RAW global _complianceFindings (filtering only _isMuted and rid==='Multiple'). Every panel/report consumer instead passes findings through _udashFilterByAccount() / _rptFilterByAccount() before counting (see _renderCompDash at line ~9434 'var findings=_udashFilterByAccount(_complianceFindings||[])' and buildComplianceView's accountFilter path in compliance-view.js). The map badge and its VPC rollup (lines 9919-9935) never apply the current global account filter, so when the user has selected a single account in multi-account mode, the badge keeps counting findings that belong to filtered-out accounts. The spotlight detail (_gatherResourceInfo, lines 9433-9436) is also account-agnostic but matches by exact f.resource===rid, so a resource ID shared/collided across accounts could still diverge.
- **Suggested fix:** Apply the same account filter the panel uses before building the badge lookup. Either (a) make `_buildComplianceLookup()` iterate `_udashFilterByAccount(_complianceFindings)` instead of the raw `_complianceFindings`, or (b) filter at the call site in `_renderComplianceBadges()` (line 9910) before iterating. The VPC rollup pass (9919-9935) reads from the same `lookup`, so filtering the source automatically fixes the rolled-up SG/NACL/RT counts too. For full consistency, do the same for the other lookup consumers at 11473 and 11992 and for the spotlight finding gather at 9434-9436 so every surface respects the active account filter. Caveat: `_buildComplianceLookup` is shared, so option (a) changes all consumers at once — confirm that is desired (it is, since all are account-scoped UI surfaces) or use option (b) to scope the fix to the map badge only.
- **Verification:** Verified by tracing the real flow (candidate's cited line numbers were offset by an rtk proxy; actual lines confirmed with sed/Read).

CONFIRMED CORE BUG:
- `_buildComplianceLookup()` (app-core.js:9885-9898) iterates the RAW global `_complianceFindings`, filtering only `_isMuted(f)` and `rid==='Multiple'`. It applies NO account filter.
- `_renderComplianceBadges()` (9905) and its VPC rollup pass (9919-9935) build all on-map badge counts from this raw lookup. The render trigger (8349 `renderMap(()=>{...;_renderComplianceBadges();...})`) is unconditional — not gated by account mode.
- By contrast the compliance panel `_renderCompDash` (actual line 435, candidate said 9434) does `var findings=_...

### 9. [MEDIUM] DOCX App Summary uses 'low' fallback instead of resource-derived bestTier, understating criticality and showing wrong RPO/RTO for auto-discovered apps

- **Domain:** exports
- **Location:** `src/exports/exports-docx.js` (lines 366 (exec, var tier=app.tier||'low'; feeds Criticality at 367) and 373-374 (technical, var tier=app.tier||'low'; meta=_TIER_RPO_RTO[tier]))
- **Impact:** For any application that does not have an explicitly set app.tier (relying on auto-classification from its resources), the DOCX report reports criticality LOW and the LOW-tier RPO/RTO, while the screen and XLSX correctly report the resource-derived tier (e.g. CRITICAL) and its RPO/RTO. Same data set, three exports disagree; DOCX understates criticality and shows incorrect recovery objectives.
- **Defect:** In _docxAppSummary both branches compute the app's criticality as `var tier=app.tier||'low'` without deriving a tier from the matched resources. The XLSX export (_rptBuildXlsxAppSummary, lines 738-741) and the on-screen HTML report (report-html.js _rptAppSummary, lines 861-863) instead compute bestTier = min tier-priority across matched resources and use `app.tier || bestTier`. In the technical branch the wrong tier then drives RPO/RTO via meta=_TIER_RPO_RTO[tier] (line 374).
- **Suggested fix:** Mirror the XLSX/HTML/_buildAppRows logic in both branches of _docxAppSummary. After `var matched=_matchAppResources(app);`, derive the best tier and use it as the fallback: `var bestTier='low'; matched.forEach(function(r){ if((tierPri[r.tier]||99)<(tierPri[bestTier]||99)) bestTier=r.tier; }); var tier=app.tier||bestTier;`. tierPri is already defined at the top of _docxAppSummary. This makes the exec Criticality column (line 367) and the technical meta=_TIER_RPO_RTO[tier] RPO/RTO (line 374) match the screen, XLSX, and HTML outputs.
- **Verification:** Verified with raw /usr/bin/sed (proxy reads were stale/mutated, so all citations were re-confirmed). In _docxAppSummary (src/exports/exports-docx.js), BOTH branches use `var tier=app.tier||'low';` with no derivation from matched resources: exec branch at line 366 (drives the Criticality column at line 367), technical branch at line 373 (drives meta=_TIER_RPO_RTO[tier] at line 374, i.e. RPO/RTO). Every other code path derives a bestTier from the matched resources' r.tier and uses `app.tier||bestTier`: XLSX _rptBuildXlsxAppSummary (exports-xlsx.js 738-741: `var bestTier='low'; matched.forEach(...r.tier...); var tier=app.tier||bestTier; meta=_TIER_RPO_RTO[tier]`), the on-screen HTML report _rpt...

### 10. [MEDIUM] Same-account ':root' trust mislabeled as cross-account in IAM Review data (prepareIAMReviewData)

- **Domain:** iam-governance
- **Location:** `src/modules/governance.js` (lines 587 (crossAccts.push(m[1]) with no own-account exclusion) and 596 (roleAcct computed but used only for _accountId))
- **Impact:** False cross-account flags. With the bundled demo data (own account 111222333444), the roles AdminRole, ReadOnlyRole, DeployRole, DataAnalystRole, and SecurityAuditRole all trust 111222333444:root (same account) and are wrongly reported as cross-account: the IAM Review tab 'Cross-Acct' column shows 1 (app-core.js:16960), the expand panel lists 111222333444 under 'Cross-Account Trusts' (app-core.js:16967), and the HTML report 'Cross-Account Trusts' KPI/table over-counts (report-html.js:1184,1190,1205-1213). Only CrossAccountRole (trusts 555666777888) is genuinely cross-account, so the count is inflated from 1 to 6.
- **Defect:** In prepareIAMReviewData, crossAccts is built (line 590) by matching EVERY 12-digit account in the trust policy's Principal.AWS via /:(\d{12}):/ and pushing it, WITHOUT excluding the role's own account. The role's own account IS computed on line 596 (`roleAcct`) but is only used for `_accountId`, never to filter crossAccts. A role whose trust policy allows its own account (e.g. Principal.AWS='arn:aws:iam::111222333444:root' on a role with Arn in 111222333444) gets its own account pushed into crossAccounts. Note runIAMChecks (iam-engine.js:89 and :112) correctly excludes `_iamOwnAccountId` for IAM-3/IAM-6, so this is an inconsistency where only the review-data path is wrong.
- **Suggested fix:** Exclude the role's own account when collecting crossAccts. Compute ownAcct before the Statement loop (reuse the line-596 regex on role.Arn), then change the push to only add when it differs: `if (m && m[1] !== ownAcct) crossAccts.push(m[1]);` — mirroring the `m && m[1] !== _iamOwnAccountId` guard already used in iam-engine.js IAM-3 (~line 89) and IAM-6 (~line 112). This makes the IAM Review tab Cross-Acct column (app-core.js:16960), the expand panel (16967), and the HTML report KPI/table (report-html.js:1184/1190/1209) report only genuine cross-account trusts (count 1 for the demo data, not 6).
- **Verification:** CONFIRMED via raw bytes. In src/modules/governance.js, prepareIAMReviewData builds crossAccts by matching every trust-policy Principal.AWS ARN with /:(\d{12}):/ and unconditionally pushing m[1] (governance.js line 587: `if (m) crossAccts.push(m[1]);`). The role's own account IS computed as roleAcct (`const roleAcct = (role.Arn || '').match(/:(\d{12}):/);`, line 596) but is only used for `_accountId`, never to filter crossAccts. crossAccounts is then `Array.from(new Set(crossAccts))`.

This is provably inconsistent with the compliance engine: iam-engine.js IAM-3 (line ~89) and IAM-6 (line ~112) both compute `_iamOwnAccountId=_acctFromArn(role)` and exclude it via `m && m[1] !== _iamOwnAccount...

### 11. [MEDIUM] buildComplianceView.mutedCount reads never-populated state.js complianceFindings instead of opts.findings — muted-count label always blank/zero

- **Domain:** multiaccount-state
- **Location:** `src/modules/compliance-view.js` (lines 349 (`mutedCount: (complianceFindings || []).filter(f => isMuted(f)).length`); import at line 30; consumer at src/app-core.js:517; caller at src/app-core.js:435 and 481)
- **Impact:** The compliance dashboard's muted-findings indicator (app-core.js:517: `mcEl.textContent=view.mutedCount>0?view.mutedCount+' finding(s) muted':''`) is always empty/zero regardless of how many findings the user has muted. Users get no feedback that findings are hidden by mutes, even though the mute filter (line 327, using the same isMuted) is actively removing them from the table — a confusing, inconsistent UI state.
- **Defect:** Inside buildComplianceView, the working set `src` is derived from `opts.findings || complianceFindings` (line 304), but the returned `mutedCount` is computed as `(complianceFindings || []).filter(f => isMuted(f)).length` (line 349), reading the module-level `complianceFindings` imported from state.js. state.js's `complianceFindings` is only ever assigned via `setComplianceFindings`, which is NEVER called anywhere in the repo (the real findings live in app-core's `_complianceFindings` / `window._complianceFindings`). So state.js's binding is permanently `[]`. The compliance dashboard caller passes the real findings explicitly (`findings:findings` at app-core.js:481, where `findings=_udashFilterByAccount(_complianceFindings||[])` at line 435), so base/filtered/sevCounts are correct, but mutedCount ignores that argument.
- **Suggested fix:** Compute mutedCount from the same source the view uses. Minimal one-line fix at compliance-view.js:349: change to `(opts.findings || complianceFindings || []).filter(f => isMuted(f)).length` so it honors the caller-supplied findings. Better: capture the pre-mute set right before `if (!opts.includeMuted) src = src.filter(f => !isMuted(f));` (e.g. `const preMute = src;`) and return `mutedCount: preMute.filter(f => isMuted(f)).length`, which also makes the count consistent with the framework/search/account pre-filters already applied to `src`.
- **Verification:** Verified the full data flow with raw sed reads (not rtk):

1. src/modules/compliance-view.js:30 — `import { complianceFindings } from './state.js';` (module-level binding).
2. src/modules/state.js:21 — `export let complianceFindings = [];` (initialized empty).
3. src/modules/state.js:35 — `export function setComplianceFindings(v) { complianceFindings = v; }` is the ONLY mutator. A repo-wide grep (`grep -rn setComplianceFindings src/`) found ZERO call sites. So state.js's binding is permanently `[]`.
4. compliance-view.js (inside buildComplianceView): working set is `let src = (opts.findings || complianceFindings || []).slice();` and the mute filter `if (!opts.includeMuted) src = src.filter(f...

### 12. [MEDIUM] BUDR Assessment table: Strategy column client-side sort mis-ranks 'Pilot Light' rows to the bottom

- **Domain:** reports-render
- **Location:** `src/modules/report-html.js` (lines 516 (comparator), 410 (STRAT_ORD), 1136 (header), 1148/1149/1155 (cell render + data-strategy attr); cross-ref budr-engine.js line 14 (_BUDR_STRATEGY) and line 15 (_BUDR_STRATEGY_ORDER))
- **Impact:** When a user clicks the 'Strategy' column header to sort the BUDR Assessment table, Pilot Light resources are always pushed to the bottom (rank 9) instead of their correct position (rank 2, between Warm and Cold). The DR-strategy ordering shown to the user is wrong specifically for the Pilot Light tier. Initial server render is correct (uses _BUDR_STRATEGY_ORDER keyed on 'pilot').
- **Defect:** rptSort's Strategy comparator (line 516) does STRAT_ORD[av.toLowerCase()] with STRAT_ORD={hot:0,warm:1,pilot:2,cold:3} (line 410). av is the Strategy cell's textContent, which is stratLabel = _BUDR_STRATEGY[strategy] (line 1148, rendered at 1155). _BUDR_STRATEGY (budr-engine.js line 14) maps pilot->'Pilot Light'. So 'Hot'/'Warm'/'Cold' lowercase to 'hot'/'warm'/'cold' which ARE keys and sort fine, but 'Pilot Light'.toLowerCase() = 'pilot light', which is NOT the key 'pilot' -> undefined -> coerced to 9. Raw-verified against budr-engine.js line 14 (_BUDR_STRATEGY={...pilot:'Pilot Light'...}).
- **Suggested fix:** In rptSort (report-html.js ~line 514), for type==='strategy' (and ideally 'tier' too) key off the row's raw dataset value rather than the rendered cell label. The row already carries data-strategy="pilot" (line 1149), so compute av/bv from `a.dataset.strategy`/`b.dataset.strategy` when type==='strategy', e.g. `var sa=(a.dataset.strategy||'').toLowerCase();` then `STRAT_ORD[sa]`. Alternatively, normalize the label by stripping ' light' (treat 'pilot light' -> 'pilot') before the STRAT_ORD lookup. Using dataset.strategy is cleanest and robust to future label wording changes.
- **Verification:** Raw-verified the full data flow in /home/schyl/gitrepos/aws_mapper/src/modules/report-html.js. Line 410: `var STRAT_ORD={hot:0,warm:1,pilot:2,cold:3};` (pilot key is 'pilot'). Line 516 comparator for type==='strategy' computes `STRAT_ORD[av.toLowerCase()]` where av is the cell's `textContent.trim()` (line 514: `a.children[idx].textContent.trim()`) — it does NOT use the row's data-strategy attribute. Line 1136 header: `<th class="rpt-sortable" data-sort-type="strategy">Strategy</th>` so clicking that column drives the strategy comparator. Lines 1148/1155: cell renders `stratLabel = _BUDR_STRATEGY[strategy]` inside `<td><span class="strategy-badge ...">stratLabel</span></td>`, so the td's text...

### 13. [LOW] Report BUDR assessment table mis-sorts 'hot' strategy to the bottom of each tier (0 || 9 falsy bug)

- **Domain:** budr
- **Location:** `src/modules/report-html.js` (lines 1124-1125)
- **Impact:** In the generated HTML report's BUDR Assessment table, the strongest-DR (hot/active-active) resources are listed last within their tier instead of first, inverting the intended strategy ordering and making the table harder to read correctly.
- **Defect:** In `_rptBUDRAssessmentTable`, the secondary (within-tier) sort key is `const sa = a.profile ? (_BUDR_STRATEGY_ORDER[a.profile.strategy] || 9) : 9;`. `_BUDR_STRATEGY_ORDER` (budr-engine.js line 15) is `{hot:0,warm:1,pilot:2,cold:3}`. For a 'hot' strategy the lookup yields `0`, and `0 || 9` evaluates to `9`, so 'hot' resources receive the worst sort priority and are placed AFTER warm/pilot/cold within the same tier. The intent is clearly the opposite (hot = best = first). Raw-verified: order map at budr-engine.js line 15; comparator at report-html.js lines 1124-1125. Within the 'protected' tier group both hot (aurora, lambda, ecs_multi, alb_multi_az, s3*) and warm (rds_multi_az, ec2_asg, ecache_multi, redshift_multi) profiles coexist, so the mis-ordering is observable.
- **Suggested fix:** Replace the `|| 9` idiom (which collapses the valid 0 value) with a nullish/explicit check, e.g.: `const sa = a.profile && _BUDR_STRATEGY_ORDER[a.profile.strategy] != null ? _BUDR_STRATEGY_ORDER[a.profile.strategy] : 9;` and the same for `sb`. Equivalently use `?? 9` after the lookup. This preserves hot=0 so hot/active-active resources sort first within their tier as intended.
- **Verification:** Raw-verified via /usr/bin/sed. report-html.js lines 1124-1125 read: `const sa = a.profile ? (_BUDR_STRATEGY_ORDER[a.profile.strategy] || 9) : 9;` and the matching `sb`. budr-engine.js line 15 (raw) defines `_BUDR_STRATEGY_ORDER={hot:0,warm:1,pilot:2,cold:3}` and it is imported into report-html.js (import on line 15, grep-confirmed). For a 'hot' profile the lookup is 0, and `0 || 9` evaluates to 9 because 0 is falsy. The comparator sorts ascending (`return sa - sb`), so within a tier hot (9) sorts AFTER warm(1), pilot(2), cold(3) — the exact inversion of intent (hot = best = first). Reproducibility confirmed: the _BUDR_RTO_RPO map (budr-engine.js lines 23-47, raw) puts both hot resources (rds...

### 14. [LOW] Effective Permissions panel keeps only the last Resource per action (multi-resource statements truncated)

- **Domain:** iam-governance
- **Location:** `src/modules/governance.js` (lines 781)
- **Impact:** The Effective Permissions section of the IAM detail panel under-reports scoped resources: an action allowed on multiple ARNs renders as if it applies to only one (the last listed), misleading the reviewer about the true blast radius.
- **Defect:** In summarizePermissions, `res.forEach(r => { services[svc].resources[actionName] = r; })` assigns into a string-valued map keyed by actionName, overwriting on each iteration. For a statement whose Resource is an array (e.g. demo CrossAccountRole: Resource:['arn:aws:s3:::shared-data','arn:aws:s3:::shared-data/*']), only the LAST resource survives. The IAM principal detail panel reads `sd.resources[a]||'*'` (app-core.js:2612) and displays just that single resource.
- **Suggested fix:** Store resources per action as a deduped array instead of a single string. Replace governance.js:781 `res.forEach(r => { services[svc].resources[actionName] = r; });` with something like `(services[svc].resources[actionName] ||= []); res.forEach(r => { if(!services[svc].resources[actionName].includes(r)) services[svc].resources[actionName].push(r); });`. Then at src/app-core.js:2612 render the joined list: `const res=(sd.resources[a]&&sd.resources[a].length)?sd.resources[a].join(', '):'*';`. (If a string-valued map must be kept, at minimum concatenate distinct resources rather than overwrite.)
- **Verification:** Raw-verified at src/modules/governance.js:781 (inside summarizePermissions, function starts line 766): `res.forEach(r => { services[svc].resources[actionName] = r; });`. resources is a string-valued map keyed by actionName, so each iteration overwrites the previous; for an array-valued Resource only the LAST element survives. The single consumer at src/app-core.js:2612 reads `const res=sd.resources[a]||'*';` and renders just that one string. Confirmed there is exactly one write site and one read site (grep verified) with no later normalization/join. Demo data at src/modules/demo-data.js:538 reproduces it exactly: CrossAccountRole inline policy `{Effect:'Allow',Action:['s3:GetObject','s3:List...

### 15. [LOW] IAM detail-panel user Admin risk-count and badge flag wildcard-Action-only (ignore Resource), diverging from canonical both-wildcard definition

- **Domain:** iam-governance
- **Location:** `src/app-core.js` (lines 3356-3357 (user Admin count), 3389-3390 (per-user Admin badge); related self-contradiction for roles only: 2604 + governance.js:775)
- **Impact:** Over-counts/over-labels principals as ADMIN. A user/role with Action:'*' restricted to a specific Resource (not true admin) is shown as ADMIN in the detail-panel risk card and per-user badge, and 'FULL ADMIN ACCESS' in Effective Permissions, inconsistent with the title badge and the IAM Review tab/IAM-1 finding which require Resource:'*' too.
- **Defect:** The IAM detail-panel risk-card ADMIN count (line 3356: `if(a.some(x=>x==='*'))uAdmin=true`, and its managed-policy twin line 3357) and the per-user ADMIN badge (lines 3389-3391) set uAdmin true when ANY action equals '*', WITHOUT also requiring a wildcard Resource. The canonical admin definition used everywhere else requires BOTH wildcard action AND wildcard resource (iam-engine.js:135 `if(uHasWildAct&&uHasWildRes)uIsAdmin=true`; governance.js:612 same). summarizePermissions (governance.js:775) has the same action-only flaw, so within one open detail panel a role with Action:'*' + scoped Resource shows NO admin badge in the title (line 2572 uses the correct principal._isAdmin) yet shows 'FULL ADMIN ACCESS (*:*)' in the Effective Permissions body (app-core.js:2603-2604) — a self-contradiction.
- **Suggested fix:** In the detail-panel USERS loop, gate uAdmin/uAdm on a wildcard Resource as well as a wildcard Action, matching iam-engine.js:135 and governance.js:612. For each Allow statement, track wildcard-action and wildcard-resource separately (e.g. const a=...; const r=Array.isArray(s.Resource)?s.Resource:[s.Resource||'']; if(a.some(x=>x==='*'||x==='*:*')&&r.some(x=>x==='*'))uAdmin=true) at lines 3356-3357 and 3389-3390. Separately, fix summarizePermissions (governance.js:775) so isAdmin requires the statement's Resource to contain '*' (currently action-only), which removes the role title-vs-body 'FULL ADMIN ACCESS' contradiction.
- **Verification:** Raw bytes (python readlines) confirm the cited lines. In the IAM detail panel's USERS loop, the Admin risk-card count sets uAdmin true on action wildcard alone: app-core.js:3356 `...const a=Array.isArray(s.Action)?s.Action:[s.Action||''];if(a.some(x=>x==='*'))uAdmin=true` and its managed-policy twin 3357 `...some(x=>x==='*'))uAdmin=true`, with NO Resource check. The per-user badge repeats this: app-core.js:3389 `if(a.some(x=>x==='*'))uAdm=true` and 3390. The canonical Admin definition requires BOTH wildcards: iam-engine.js:53 `if(hasWildAction&&hasWildResource)role._isAdmin=true`; iam-engine.js:135 `if(uHasWildAct&&uHasWildRes)uIsAdmin=true`; governance.js:612/614 require `a.some(x==='*') &&...

### 16. [LOW] Imported IAM findings render an 'undefined' severity badge in the IAM Review tab

- **Domain:** iam-governance
- **Location:** `src/app-core.js` (lines 8686 (synthetic findings, function _parseFindingsCount 8683-8688); rendered at 16977 (color ternary 16976))
- **Impact:** After importing a previously-generated HTML report, expanding an IAM principal that had findings shows badges reading 'undefined' instead of a severity label.
- **Defect:** _parseFindingsCount (app-core.js:8683-8688) reconstructs imported finding counts as objects `{message:'Imported finding'}` with NO `severity` field. When the IAM Review tab expands a row for an imported report, app-core.js:16975-16977 computes `f.severity==='CRITICAL'?...:'#3b82f6'` (falls through to the low-severity color, acceptable) but then renders the literal `f.severity` text into the badge: `...>'+f.severity+'</span>'`, producing the string 'undefined'.
- **Suggested fix:** Two viable fixes: (1) Give synthetic findings a default severity in _parseFindingsCount, e.g. `arr.push({message:'Imported finding',severity:'LOW'})` at line 8686 — this also makes the blue color intentional rather than incidental. OR (2) Guard the render at 16977 with `(f.severity||'LOW')` (or hide the badge entirely when severity is absent: `(f.severity?'<span ...>'+f.severity+'</span>':'')`). Fix (1) is cleaner since it fixes the data at the source and any other consumer of imported findings.
- **Verification:** Verified the full data flow via raw bytes (sed/python3), all line numbers confirmed against actual file content.

IMPORT PATH (buggy): _parseFindingsCount (app-core.js:8683-8688) reconstructs imported finding counts as `arr.push({message:'Imported finding'})` for i in 0..n — these objects have NO `severity` field. _parseReportIAM (8613) attaches them to result.iamReviewData[].findings at lines 8632, 8652, 8672. _applyImportedData (8408-8418) copies these objects into the global _iamReviewData verbatim (it only guards `if(!Array.isArray(copy.findings))copy.findings=[]`, preserving the severity-less objects).

RENDER (raw-confirmed line 16977): _renderIAMTab (16877) builds the expandable detai...

### 17. [LOW] findAlternatePaths adds the source ALB to its own candidate list (missing source-skip), yielding self-referential "via source" alternate paths

- **Domain:** map-flow
- **Location:** `src/modules/flow-tracing.js` (lines 611 (module copy); src/app-core.js:13322 (live copy actually invoked via 13034))
- **Impact:** When the user traces a flow whose source is an ALB and the direct path is blocked, the suggested alternate paths can include a self-referential route (source ALB -> source ALB -> target). leg1 traces the source ALB to itself and trivially succeeds, producing a meaningless/duplicate 'via <source>' suggestion that pollutes the alternate-path list shown to the user.
- **Defect:** In findAlternatePaths, the instance-candidate loop skips a candidate that matches the target (line 598) AND a candidate that matches the source (line 599). The ALB-candidate loop, however, only skips when the ALB matches the target: `if (albId === (target.type === 'alb' ? target.id : '')) return;` (line 611). There is no corresponding `if (albId === (source.type === 'alb' ? source.id : '')) return;` guard. So when the flow source is an ALB, that same ALB is added to the candidate list and may be selected as an intermediary 'via' hop.
- **Suggested fix:** Mirror the instance-loop guard in both copies. In flow-tracing.js after line 611 add: `if (albId === (source.type === 'alb' ? source.id : '')) return;` — and account for the LoadBalancerName fallback used as the candidate id, e.g. guard on `(albId || alb.LoadBalancerName) === (source.type === 'alb' ? source.id : '')`. Apply the identical guard in src/app-core.js _findAlternatePaths after line 13322, since that is the function the running app actually calls.
- **Verification:** Verified the candidate's three load-bearing lines with awk (raw): flow-tracing.js:598 skips the target instance, :599 skips the source instance, but :611 (`if (albId === (target.type === 'alb' ? target.id : '')) return;`) skips only the target ALB — there is no `if (albId === (source.type === 'alb' ? source.id : '')) return;`. The same bug exists in the live code path: the actual caller is _findAlternatePaths in src/app-core.js (invoked at line 13034 when _flowBlocked), whose ALB loop at 13322 skips only the target (13310/13311 skip both for instances). Reachability confirmed: clicking an ALB node yields {type:'alb',id:...} (app-core.js:12619) which becomes _flowSource. When source is ALB X,...

### 18. [LOW] Top tabs are covered by the left sidebar instead of shifting when it opens (map view)

- **Domain:** ui-layout
- **Location:** `(UI/CSS - locate the report/dashboard top-tab bar + .main left offset)`
- **Impact:** After loading data and opening Reports, the top tab bar is overlapped by the open left sidebar on the map view instead of shifting right.
- **Defect:** Reported by user; the top-tab container's left offset/width does not account for the open left sidebar width.
- **Suggested fix:** Offset the tab bar by the sidebar width (or place it inside the content flow) when the sidebar is open; mirror whatever the map canvas uses for its left inset.
- **Verification:** User-reported UI bug (not part of the code-agent sweep).
